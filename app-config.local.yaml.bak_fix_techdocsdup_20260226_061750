# Dev-only overrides to expose Backstage UI to Windows via WSL IP.
# WSL IP (measured): 172.21.211.225

app:
  baseUrl: http://172.21.211.225:3000

backend:
  auth:
    dangerouslyDisableDefaultAuthPolicy: true
  baseUrl: http://172.21.211.225:7007
  cors:
    origin: http://172.21.211.225:3000

techdocs:
  generator:
    runIn: local


# === FS SAFE PATCH: ensure techdocs-smoke entity is discoverable in RUN repo ===
catalog:
  locations:
    - type: file
      target: file:/home/danie/control_plane/backstage/packages/backend/components/techdocs-smoke/catalog-info.yaml

# === FS SAFE PATCH: TechDocs local publisher (RUN repo) ===
techdocs:
  publisher:
    type: local
    local:
      publishDirectory: /home/danie/control_plane/backstage/packages/backend/static/docs
